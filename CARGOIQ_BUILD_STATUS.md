# CargoIQ Investigative Evidence

Build gate: API TypeScript + PostgreSQL migration + tests + Docker.

BrowserSkill is an adapter executed by a controlled worker; it is not required by the core API build.
